# Runtime deps for `f2` (Douyin, a_bogus signing).
#
# `f2` is installed with --no-deps because it pins httpx==0.27, pydantic==2.9.*
# and websockets<13, all of which conflict with the web stack in
# requirements.txt. These are the packages f2 actually imports at runtime, minus
# the ones requirements.txt already provides (httpx, pydantic, websockets).
#
#   pip install -r requirements.txt
#   pip install -r requirements-douyin.txt
#   pip install --no-deps 'f2>=0.0.1'

aiofiles>=24.1
aiosqlite>=0.20
babel>=2.13
browser-cookie3>=0.20
click>=8.1
cryptography>=44.0
gmssl>=3.2
importlib-resources>=6.4
jsonpath-ng>=1.6
m3u8>=3.6
protobuf>=5.28
pyexecjs>=1.5
pyyaml>=6.0
qrcode>=8.0
rich>=13.9
websockets-proxy>=0.1
